services:
  - type: web
    name: devops-course
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start:prod
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: devops-course
          property: connectionString